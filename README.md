# 🔐 Secure Password Generator

A simple and powerful **secure password generator** built using Python’s `secrets` module, which is suitable for cryptographic use.

## 🚀 Features
- Generate strong random passwords
- Adjustable length
- Choose which character types to include:
  - Uppercase letters
  - Lowercase letters
  - Digits
  - Symbols
- Uses Python’s secure random generator (`secrets`)

---

